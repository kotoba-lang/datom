# datom-clj

kotoba Datom-log (EAVT, Datomic-isomorphic) representation. **Purpose: data representation.** Shared by every actor that datafies results (vehicle-design-actor, aero-clj, the CAE seeds).

Part of the clean-sheet vehicle-design / CAE stack (purpose-split shared libs).
Zero-dep portable `.cljc`. Run `clojure -M:test`.
